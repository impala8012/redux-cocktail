export const selectPosts = (stores) => stores.post.posts;
export const selectPost = (stores) => stores.post.post;
export const selectErrorMessage = (stores) => stores.post.errorMessage;
export const selectIsLoading = (stores) => stores.post.isLoading;
export const selectPages = (stores) => stores.post.pages;
